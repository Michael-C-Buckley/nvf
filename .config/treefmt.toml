# Stick with the main project files for now
excludes = ["*.lock", ".gitignore", ".envrc", "npins/**", ".config/**", ".github/**"]
on-unmatched = "warn"

[formatter.alejandra]
command = "alejandra"
includes = ["*.nix"]

[formatter.mdformat]
command = "mdformat"
includes = ["*.md"]
